% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_loglik.R
\name{fast_loglik}
\alias{fast_loglik}
\title{Fast Conditional Log-Likelihood (loglik only)}
\usage{
fast_loglik(X, choice, strata, offset = NULL, max_iter = 5L, tol = 0.001)
}
\arguments{
\item{X}{Numeric matrix (n x p). Design matrix, no intercept.}

\item{choice}{Integer vector (n). 1 for chosen alternative, 0 otherwise.}

\item{strata}{Vector (n). Choice-set / stratum identifier.}

\item{offset}{Numeric vector (n) or NULL. Fixed offset in linear predictor.}

\item{max_iter}{Integer. Maximum Newton-Raphson iterations (default 5).}

\item{tol}{Numeric. Convergence tolerance (default 1e-3).}
}
\value{
Numeric vector of length 2: \code{c(loglik_null, loglik_fitted)}.
  \code{loglik_null} is the log-likelihood at beta=0 (with offset if provided).
  \code{loglik_fitted} is the log-likelihood at the MLE.
}
\description{
Computes the null and fitted conditional log-likelihood via C++ Newton-Raphson.
Returns only the two loglik values — no coefficients, variance, or model object.
}
\details{
Designed for CLogitTree split evaluation where hundreds of candidate splits
are tested and only the likelihood-ratio statistic matters.
}
\examples{
sim <- simulate_clogit_data(n_egos = 100, n_alts = 10)
ll <- fast_loglik(sim$X, sim$choice, sim$strata)
cat("Null LL:", ll[1], "  Fitted LL:", ll[2], "\n")

}
