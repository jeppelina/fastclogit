% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{tidy_fastclogit}
\alias{tidy_fastclogit}
\title{Extract results as a tidy data.frame}
\usage{
tidy_fastclogit(object, robust = TRUE, conf.level = 0.95, exponentiate = FALSE)
}
\arguments{
\item{object}{A \code{fastclogit} object.}

\item{robust}{Use robust (clustered sandwich) SEs if available? Default
\code{TRUE}.}

\item{conf.level}{Confidence level for intervals. Default 0.95.}

\item{exponentiate}{Return odds ratios instead of log-odds? Default
\code{FALSE}.}
}
\value{
A data.frame with columns: \code{term}, \code{estimate},
  \code{std.error}, \code{statistic}, \code{p.value}, \code{conf.low},
  \code{conf.high}.
}
\description{
Returns coefficients, SEs, z-values, p-values, and CIs in a data.frame.
Compatible with broom-style workflows.
}
\details{
Includes a safety check: if \code{names(object$coefficients)} is
\code{NULL} (can happen when post-fit operations like unscaling strip
names), falls back to \code{object$terms}, then to V1, V2, ...
}
